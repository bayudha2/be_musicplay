const UserRepository = require('../../../Domains/users/UserRepository');
const UpdateProfilImageUseCase = require('../UpdateProfilImageUseCase');
const StorageHelper = require('../../utils/StorageHelper');

describe('UpdateProfilImage', () => {
  it('should orchestrating the update user correctly', async () => {});
});
